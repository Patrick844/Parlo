# Serve the built SPA; every unknown path falls back to index.html
# so react-router can handle client-side routes like /f/:slug.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
